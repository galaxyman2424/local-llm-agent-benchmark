# Codebase Navigation (Graft)
- When starting a task or analyzing the codebase, check if the `graft/` directory exists.
- Read `graft/index.md` or relevant files in `graft/` to understand component relationships and code architecture before searching files manually.
- Use the structural code map in `graft/` to skip unnecessary file exploration.
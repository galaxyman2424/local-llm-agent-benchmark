name: qwen_ornith

planner:
  model: qwen3.5:9b
  timeout_seconds: 300  # max seconds for a single Ollama request
  num_ctx: 131072 

actioner:
  model: ornith:9b
  timeout_seconds: 300  # max seconds for a single Ollama request
  num_ctx: 131072
  max_read_lines: 300

agent:
  max_iterations: 50
  timeout: 600  # max seconds for the WHOLE task (all iterations), not one LLM call